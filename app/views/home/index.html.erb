<section class='home-page'>   <!-- TODO: use id instead of class -->
  
  <% if current_user %>
    <h1 id='user-name'></h1>
    <h2 id='user-title'></h2>
    <p id='user-email' class='email'></p>
    <table id='user-highlights'></table>

    <table id='user-jobs' class='user-jobs'>
      <!-- <tr class='user-job'>
        <td class='left-column'>
          <p class='company'>Acme Software Inc.</p>
          <p class='date-range'>1999-2009</p>
          <p class='role'>Senior Developer</p>
        </td>
        <td class='right-column'>
          <ul class='tasks'>
            <li class='task'>wrote 10K unit tests</li>
            <li class='task'>refactored unit tests and reduced their number by 99%</li>
            <li class='task'>wrote 20K units tests</li>
            <li class='task'>managed the procurement of 18 Cray supercomputers</li>
            <li class='task'>aquired a hydro power generation plant</li>
            <li class='task'>C, C++, Assembler</li>
          </ul>
        </td>
      </tr> -->
    </table>

  <% else %>
    <h1>Biodb</h1>

    <nav>
      <%= link_to 'sign in', new_session_path %>
      <%= link_to 'sign up', new_user_path %>
    </nav>
  <% end %>

</section>


<% if current_user %>
  <script>
    user = {
      id: "<%= current_user.id %>",
      name: "<%= current_user.name %>",
      title: "<%= current_user.title %>",
      email: "<%= current_user.email %>",
      highlights: <%= (current_user.highlights.nil? ? '[]' : current_user.highlights.html_safe) %>,         // TODO: see warning comment at http://stackoverflow.com/a/3758055
      jobs: <%= (current_user.jobs.nil? ? '[]' : current_user.jobs.html_safe) %>
    };
  </script>
<% end %>
